---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';
import cargaSeca from '../assets/cargaSeca.jpg';
import contenedor from '../assets/container2.png';

const logoModules = import.meta.glob<{ default: ImageMetadata }>('../assets/logos/*.{jpg,JPG}', {
	eager: true,
});
const iconModules = import.meta.glob<{ default: ImageMetadata }>('../assets/icons/*.png', {
	eager: true,
});

function logo(path: string) {
	const mod = logoModules[`../assets/logos/${path}`];
	if (!mod) throw new Error(`Logo no encontrado: ${path}`);
	return mod.default;
}

function icon(path: string) {
	const mod = iconModules[`../assets/icons/${path}`];
	if (!mod) throw new Error(`Icono no encontrado: ${path}`);
	return mod.default;
}

const pasos = [
	{
		titulo: 'Compra a fabricantes aliados',
		descripcion: 'Negociamos directamente con múltiples fabricantes colombianos.',
	},
	{
		titulo: 'Recepción en nuestra bodega',
		descripcion: 'Recibimos y verificamos la mercancía en nuestras instalaciones especializadas.',
	},
	{
		titulo: 'Etiquetado según país destino',
		descripcion: 'Adaptamos el etiquetado e información a las normativas de cada mercado.',
	},
	{
		titulo: 'Embalaje especializado',
		descripcion: 'Usamos el empaque adecuado para proteger cada producto durante el transporte.',
	},
	{
		titulo: 'Consolidación de carga',
		descripcion: 'Agrupamos diferentes productos de varios proveedores en un solo envío.',
	},
	{
		titulo: 'Gestión documental y aduanera',
		descripcion: 'Nos encargamos de toda la documentación, permisos e inspecciones.',
	},
	{
		titulo: 'Exportación internacional',
		descripcion: 'Coordinamos el transporte internacional hasta el puerto de destino.',
	},
];

const marcas = [
	{ nombre: 'Colcafé', src: logo('colcafe.jpg') },
	{ nombre: 'Café La Bastilla', src: logo('cafe.jpg') },
	{ nombre: 'Chocolate Corona', src: logo('corona.jpg') },
	{ nombre: 'Chocolate', src: logo('chocolate.jpg') },
	{ nombre: 'Choco Listo', src: logo('choco.jpg') },
	{ nombre: 'Saltín Noel', src: logo('saltin.jpg') },
	{ nombre: 'Ducales', src: logo('ducales.jpg') },
	{ nombre: 'Universal', src: logo('universal.jpg') },
	{ nombre: 'Doria', src: logo('doria.jpg') },
	{ nombre: 'Deli', src: logo('deli.jpg') },
	{ nombre: 'Jet', src: logo('jet.jpg') },
	{ nombre: 'Jumbo', src: logo('jumbo.jpg') },
];

const pistaMarcas = [...marcas, ...marcas];

const beneficios = [
	{ label: 'Menor inversión en inventario', icon: icon('dinero.png') },
	{ label: 'Reducción de costos logísticos', icon: icon('precio-bajo.png') },
	{ label: 'Más productos, más oportunidades', icon: icon('agregar-producto.png') },
	{ label: 'Seguridad en cada etapa del proceso', icon: icon('privacidad-de-datos.png') },
	{ label: 'Acceso a nuevos mercados', icon: icon('acceso-a-internet.png') },
	{ label: 'Trazabilidad y control total', icon: icon('prueba.png') },
];
---

<section id="carga-seca" class="bg-transparent py-10 sm:py-10">
	<div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
		<div class="carga-hero relative overflow-hidden rounded-2xl sm:rounded-3xl">
			<Image
				src={cargaSeca}
				alt="Línea de carga seca — consolidación y exportación GTM Alliance"
				width={854}
				height={352}
				widths={[640, 854]}
				sizes="(min-width: 1024px) 1152px, 100vw"
				quality={92}
				loading="lazy"
				class="block h-auto w-full"
			/>
<<<<<<< HEAD
			<div class="absolute left-0 top-0 z-10 max-w-lg px-4 pt-5 pb-6 text-left sm:max-w-xl sm:px-8 sm:pt-6 lg:max-w-2xl lg:px-10 lg:pt-8">
				<h2 class="carga-title font-display text-3xl leading-[1.05] font-bold text-white [text-shadow:0_2px_14px_rgba(10,18,32,0.9)] sm:text-4xl lg:text-5xl">
					<span class="block uppercase">Línea de</span>
					<span class="block uppercase text-gtm-gold">Carga Seca</span>
=======
			<div class="pointer-events-none absolute inset-0 z-10 bg-gradient-to-br from-black/75 via-black/35 to-transparent"></div>
			<div class="absolute left-0 top-0 z-20 max-w-lg px-4 pt-5 pb-6 text-left sm:max-w-xl sm:px-8 sm:pt-6 lg:max-w-2xl lg:px-10 lg:pt-8">
				<h2 class="carga-title font-display text-3xl leading-[1.05] font-bold text-white drop-shadow-md sm:text-4xl lg:text-5xl">
					<span class="block">Línea de</span>
					<span class="block text-gtm-gold">Carga Seca</span>
>>>>>>> edb21c2c83d589e832a3fc9fc0f8fb46e9478d65
				</h2>
				<p class="carga-intro mt-2.5 text-sm leading-relaxed text-slate-100 [text-shadow:0_1px_10px_rgba(10,18,32,0.85)] sm:mt-3 sm:text-base lg:text-lg">
					Consolidamos productos de múltiples
					<span class="block">fabricantes en un solo embarque internacional.</span>
				</p>
			</div>
		</div>

		<!-- Stepper: Así funciona GTM -->
		<div class="carga-stepper mt-12 sm:mt-16">
			<div class="flex items-center justify-between gap-4">
				<h3 class="font-display text-2xl font-bold text-gtm-navy sm:text-3xl">Así funciona GTM</h3>
				<div class="hidden shrink-0 gap-2 lg:flex">
					<button
						type="button"
						class="carga-paso-prev flex h-11 w-11 items-center justify-center rounded-full border border-gtm-navy/15 bg-white text-gtm-navy transition-colors hover:bg-gtm-navy/5 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gtm-gold"
						aria-label="Paso anterior"
					>
						<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="h-5 w-5">
							<path d="M15 6l-6 6 6 6" />
						</svg>
					</button>
					<button
						type="button"
						class="carga-paso-next flex h-11 w-11 items-center justify-center rounded-full border border-gtm-navy/15 bg-white text-gtm-navy transition-colors hover:bg-gtm-navy/5 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gtm-gold"
						aria-label="Paso siguiente"
					>
						<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="h-5 w-5">
							<path d="M9 6l6 6-6 6" />
						</svg>
					</button>
				</div>
			</div>

			<ol
				class="carga-paso-track mt-6 flex snap-x snap-mandatory gap-4 overflow-x-auto pb-2 [scroll-padding-inline:1rem] sm:mt-8 sm:gap-5 [mask-image:linear-gradient(to_right,transparent,black_2%,black_96%,transparent)]"
				tabindex="0"
				role="region"
				aria-label="Proceso de consolidación de carga seca"
			>
				{
					pasos.map((paso) => (
						<li class="carga-paso flex w-[15rem] shrink-0 snap-start flex-col gap-2 rounded-2xl border border-gtm-gold/40 bg-white p-4 shadow-sm shadow-gtm-navy/5 sm:w-[17rem] sm:gap-3 sm:p-5 lg:w-[16.5rem]">
							<h4 class="font-display text-base font-bold leading-snug text-gtm-navy">{paso.titulo}</h4>
							<p class="text-sm leading-relaxed text-slate-600">{paso.descripcion}</p>
						</li>
					))
				}
			</ol>
		</div>

		<!-- Carrusel de marcas -->
		<div class="carga-marcas mt-12 sm:mt-16">
			<h3 class="font-display text-xl font-bold leading-tight text-gtm-navy sm:text-2xl">
				Marcas colombianas que consolidamos
			</h3>

			<div
				class="carga-marquee group relative mt-6 min-w-0 overflow-hidden sm:mt-8 [mask-image:linear-gradient(to_right,transparent,black_8%,black_92%,transparent)]"
			>
				<div class="carga-marquee-track flex w-max items-center">
					{
						pistaMarcas.map((marca, i) => {
							const decorativo = i >= marcas.length;
							return (
								<span
									class="mr-3 flex h-14 shrink-0 items-center justify-center rounded-xl bg-white px-4 ring-1 ring-gtm-navy/10 sm:mr-6 sm:h-24 sm:rounded-2xl sm:px-8"
									aria-hidden={decorativo ? 'true' : undefined}
								>
									<Image
										src={marca.src}
										alt={decorativo ? '' : marca.nombre}
										height={56}
										densities={[1, 2]}
										loading="lazy"
										class="h-8 w-auto max-w-[7rem] object-contain opacity-90 sm:h-12 sm:max-w-[9rem]"
									/>
								</span>
							);
						})
					}
				</div>
			</div>
		</div>
	</div>
</section>

<script>
	import { gsap } from '../scripts/gsap';

	const section = document.querySelector('#carga-seca');

	if (section) {
		const mm = gsap.matchMedia();
		const marquee = section.querySelector('.carga-marquee');
		const track = section.querySelector<HTMLElement>('.carga-marquee-track');
		const pasoTrack = section.querySelector<HTMLElement>('.carga-paso-track');
		const pasoPrev = section.querySelector<HTMLButtonElement>('.carga-paso-prev');
		const pasoNext = section.querySelector<HTMLButtonElement>('.carga-paso-next');

		const scrollPasos = (direction: 1 | -1) => {
			if (!pasoTrack) return;
			const card = pasoTrack.querySelector<HTMLElement>('.carga-paso');
			const distance = (card?.offsetWidth ?? 240) + 20;
			pasoTrack.scrollBy({ left: direction * distance, behavior: 'smooth' });
		};
		const goPrev = () => scrollPasos(-1);
		const goNext = () => scrollPasos(1);
		pasoPrev?.addEventListener('click', goPrev);
		pasoNext?.addEventListener('click', goNext);

		mm.add('(prefers-reduced-motion: no-preference)', () => {
			const tl = gsap.timeline({
				defaults: { ease: 'power3.out' },
				scrollTrigger: {
					trigger: section,
					start: 'top 75%',
				},
			});

			tl.fromTo('.carga-hero', { opacity: 0, y: 32 }, { opacity: 1, y: 0, duration: 0.7 })
				.fromTo('.carga-title', { opacity: 0, y: 24 }, { opacity: 1, y: 0, duration: 0.7 }, '-=0.35')
				.fromTo('.carga-intro', { opacity: 0, y: 24 }, { opacity: 1, y: 0, duration: 0.7 }, '-=0.5')
				.fromTo('.carga-stepper', { opacity: 0, y: 32 }, { opacity: 1, y: 0, duration: 0.7 }, '-=0.35')
				.fromTo('.carga-paso', { opacity: 0, y: 16 }, { opacity: 1, y: 0, duration: 0.4, stagger: 0.08 }, '-=0.3')
				.fromTo('.carga-marcas', { opacity: 0, y: 32 }, { opacity: 1, y: 0, duration: 0.6 }, '-=0.2')
				.fromTo('.carga-beneficios', { opacity: 0, y: 32 }, { opacity: 1, y: 0, duration: 0.6 }, '-=0.3')
				.fromTo('.carga-cta', { opacity: 0, y: 28 }, { opacity: 1, y: 0, duration: 0.6 }, '-=0.3');

			let marqueeTween: gsap.core.Tween | undefined;
			const pause = () => marqueeTween?.pause();
			const resume = () => marqueeTween?.resume();

			if (marquee && track) {
				marqueeTween = gsap.to(track, {
					xPercent: -50,
					duration: 25,
					ease: 'none',
					repeat: -1,
				});
				marquee.addEventListener('mouseenter', pause);
				marquee.addEventListener('mouseleave', resume);
			}

			return () => {
				tl.scrollTrigger?.kill();
				tl.kill();
				marqueeTween?.kill();
				marquee?.removeEventListener('mouseenter', pause);
				marquee?.removeEventListener('mouseleave', resume);
			};
		});

		mm.add('(prefers-reduced-motion: reduce)', () => {
			gsap.set(
				[
					'.carga-hero',
					'.carga-title',
					'.carga-intro',
					'.carga-stepper',
					'.carga-paso',
					'.carga-marcas',
					'.carga-beneficios',
					'.carga-cta',
				],
				{ opacity: 1, y: 0 },
			);
			if (track) gsap.set(track, { xPercent: 0 });
		});
	}
</script>

<style>
	.carga-paso-track {
		scrollbar-width: none;
	}

	.carga-paso-track::-webkit-scrollbar {
		display: none;
	}

	@media (prefers-reduced-motion: no-preference) {
		.carga-paso-track {
			scroll-behavior: smooth;
		}
	}
</style>
